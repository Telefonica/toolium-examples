# temporal requirement until toolium 3.2.0 is published
git+https://git@github.com/Telefonica/toolium@feat/playwright
# toolium~=3.2
behave==1.2.6  # behave tests
pytest==7.2    # pytests tests
nose2==0.12.0  # nose2 tests
ddt==1.6.0     # data-driven in nose2 tests
# temporal requirement until toolium includes playwright with pytest
pytest-playwright==0.4.4  # playwright tests
